#include "Mono.h"

// Mono::Mono()
// {
//     Engine::AddMono("test", this); //TODO Fix this to a proper name, also you want to call this on the derived object rather than this instance, as part of the component incitalization
// }
